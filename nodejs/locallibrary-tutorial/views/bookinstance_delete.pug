extends layout

block content
  h1 ID: #{instance._id}
  p #[strong Are you sure that you want to delete this book instance?]
  form(method="POST", action="")
    div.form-group
      input#id.form-control(type="hidden", name="id", value=instance._id, require="true")
    button.btn.btn-primary(type="submit") Delete