extends layout

block content
  h1 ID: #{genre._id}
  h3 Books
  each book in books
    a(href=book.url) #{book.title} 
    p #{book.summary}

  hr
  p
    a(href=genre.url + "/update") Update
  p
    a(href=genre.url + "/delete") Delete
  